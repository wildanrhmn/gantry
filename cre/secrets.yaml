secretsNames:
  # The thresholds that decide a tier. The workflow is public; these are not.
  gantry_scoring_params:
    - GANTRY_SCORING_PARAMS
  # Auth for the features endpoint, so the enclave can read indexed history.
  gantry_features_key:
    - GANTRY_FEATURES_KEY
